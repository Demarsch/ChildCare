<Window x:Class="Registry.EditPersonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:registry="clr-namespace:Registry"
        Width="610" SizeToContent="Height">
    <Grid>
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition  Height="auto"/>
        </Grid.RowDefinitions>

        <registry:EditPersonDataUserControl  Grid.Row="0" Grid.Column="0" x:Name="personEditor" DataContext="{Binding EditPersonDataViewModel, Mode=OneWay}"/>
        <StackPanel Grid.Row="0" Grid.Column="1" Width="30">
            <Button Content="+"/>
        </StackPanel>
        <registry:EditPersonDataUserControl  Grid.Row="0" Grid.Column="3" x:Name="personRelativeEditor"/>
    </Grid>
</Window>
