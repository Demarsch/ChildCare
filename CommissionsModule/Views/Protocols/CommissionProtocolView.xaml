<UserControl x:Class="CommissionsModule.Views.CommissionProtocolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels ="clr-namespace:CommissionsModule.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:CommissionProtocolViewModel}">
    <Grid>
        <DockPanel>
            <ContentControl DockPanel.Dock="Top" Content="{Binding PreliminaryProtocolViewModel}"/>
            <ContentControl DockPanel.Dock="Top" Content="{Binding CommissionСonductViewModel}"/>
            <ContentControl DockPanel.Dock="Top" Content="{Binding CommissionСonclusionViewModel}"/>
        </DockPanel>
    </Grid>
</UserControl>
