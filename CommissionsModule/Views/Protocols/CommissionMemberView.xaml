<ResourceDictionary x:Class="CommissionsModule.Views.CommissionMemberView"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:CommissionsModule.ViewModels">
    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <DataTemplate DataType="{x:Type viewModels:CommissionMemberViewModel}" >

        <StackPanel Orientation="Vertical" Background="#FFBBCFBB">
            <TextBlock Text="{Binding MemberTypeName}" FontStyle="Italic"/>
            <TextBlock Text="{Binding StaffName}" FontWeight="Bold"/>
            <TextBlock Text="{Binding PersonName}" FontWeight="Bold"/>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>
