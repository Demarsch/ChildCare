<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                    xmlns:viewModels="clr-namespace:ScheduleModule.ViewModels">
    <DataTemplate DataType="{x:Type viewModels:MultiAssignRecordTypeViewModel}">
        <Border HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch"
                Background="White"
                BorderBrush="DarkGoldenrod"
                BorderThickness="1"
                x:Name="border" 
                CornerRadius="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Path=RecordTypeName}"/>
                <Button Grid.Row="1" Command="{Binding AddRoomFilter}" Content="{Binding SelectedRoom}" BorderThickness="0"/>
                <Button Grid.Row="2" Command="{Binding AddDoctorFilter}" Content="{Binding SelectedDoctor}" BorderThickness="0"/>
            </Grid>
        </Border>
    </DataTemplate>
</ResourceDictionary>